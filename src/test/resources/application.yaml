page:
  exchangeratesapi.io:
    base-currency: AUD
    base-url: http://localhost:8080
    access-key: ACCESS_KEY

spring:
  datasource:
    url: jdbc:h2:mem:pagedb
  sql:
    init:
      mode: always
